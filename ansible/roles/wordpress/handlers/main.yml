---
- name: Перезагрузить веб Nginx
  ansible.builtin.systemd_service:
    name: nginx
    state: reloaded

- name: Перезапустить PHP-FPM
  ansible.builtin.systemd_service:
    name: "php{{ php_version }}-fpm"
    state: restarted
    daemon_reload: true
